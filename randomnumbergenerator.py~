import random,tkinter
import os,sys,turtle
def init():
	t=turtle.pen
	x=True
	print("1")
	k=input("Type something to see if this works")
	b=print("You typed " + k)
	d=input("If that is correct please retype if not press return")
	if d==k:
		print("Done, everything looks good here.\n Time to run")
	else:
		print("No, something went wrong.\n Please restart the app or your system.")
		input("Restart")
		sys.exit(0)

init()
#App intalized
Number_of_numbers= input("How many random number would you like")
para1= input("Please enter the smallest value for your number")
para2= input("Please enter the largest value for your number")
print("Para1 is %(para1)s and para2 is %(para2)s"%locals())
if para1 is None:
	print("No value entered for para1")
	system.exit()
if para2 is None:
	print("No value entered for para2")
#Parameters for random numbers entered
def random_number():
	the_random_number= random.randint(para1,para2)
	the_random_number=int(the_random_number)

def get_random_number(the_random_number):
	y=the_random_number

def check_random_number(the_random_number):
	if the_random_number== the_random_number():
		pass
	else:
		print("Please restart the program something went wrong")
#Loaded random number functions	
random_number()
get_random_number(the_random_number)
check_random_number(the_random_number)
for x in range(Number_of_numbers):
        random_number()
        get_random_number(random_number)
        check_random_number(random_number)
        print(y)
#Got random numbers
print(y)
input("Was that random number ok?")
#Get str to num Error resolved

#################################################################################
#Gui


















